# 엔드포인트 파라미터 점검 절차

새 모델이나 새 엔드포인트를 도입할 때, 어떤 파라미터가 실제로 먹히는지 확인하는 절차. 프록시는 파라미터를 조용히 무시하거나 라우트 레벨에 값을 박아두는 경우가 있으므로, 문서를 믿지 말고 실측한다.

확인 결과는 `model-catalog.md`에 기록한다. 절차는 `probe.sh`가 자동화한다.

---

## 1. 절차

### 1단계 — 메타데이터 확인

```bash
curl -s $BASE/models -H "Authorization: Bearer $KEY" | jq '.data[0]'
```

먼저 원본 JSON을 까서 필드명을 확인한다. 엔드포인트에 따라 표준 밖 필드(rpm, 툴콜 지원 여부 등)가 실려 오며, 필드명은 구현마다 다르다. 확인한 뒤에 표로 뽑는다.

### 2단계 — 허용값 캐기

파라미터에 존재하지 않는 값을 넣고 에러 메시지를 읽는다.

```bash
-d '{..., "reasoning_effort": "__probe__"}'
```

검증하는 구현이면 허용 목록을 그대로 뱉는다. **문법을 추측하는 것보다 이 방법이 빠르고 확실하다.** 공식 문서나 OpenRouter의 값 체계는 프록시마다 다르므로 근거로 쓰지 않는다.

에러가 없으면 두 가지 중 하나다. 검증을 안 하거나, 파라미터가 필터링되어 라우트까지 도달하지 않거나. 후자면 이후 단계는 의미가 없다.

### 3단계 — 파라미터별 스윕

허용값을 하나씩 넣고 응답 지표를 비교한다. 값이 실제로 반영되는지는 **지표가 단조 변화하는지**로 판정한다. 값마다 결과가 비슷하면 무시되는 중이다.

### 4단계 — 기록

`model-catalog.md`에 항목을 추가한다.

---

## 2. 판정 기준

### 프롬프트 선정

점검용 프롬프트는 **부하가 실제로 걸리는 것**을 쓴다. 가벼운 질문은 단계 차이를 만들어내지 못해 판정이 불가능하다. 정답이 하나로 수렴하는 질문도 피한다.

셸 변수로 프롬프트를 넘길 때는 `jq -n --arg`로 body를 만든다. 문자열 보간은 따옴표가 깨지고, 변수가 비면 빈 프롬프트가 그대로 나가서 측정이 통째로 무효가 된다.

### 추론 파라미터

응답에서 추론 텍스트를 꺼내 길이를 잰다. 필드명은 구현마다 다르므로 전부 훑는다.

```
.choices[0].message.reasoning
.choices[0].message.reasoning_content
.choices[0].message.thinking
content 안의 <think>...</think> 태그
```

| 관찰 | 판정 |
|---|---|
| 최소값에서 길이 0, 이후 단조 증가 | 작동 |
| 값마다 비슷 | 무시됨. 라우트 고정값 |
| 전부 0 | 비추론 모델이거나 라우트가 추론 OFF 고정 |

`completion_tokens`는 본문 길이 변동이 섞이므로 보조 지표로만 쓴다. 판정은 추론 길이로 한다.

### 온도

**최종 출력 하나로 판정하지 않는다.** 추론 모델은 생각을 거쳐 최빈답으로 수렴하므로, 온도가 높아도 같은 답이 나온다.

**답 세기**를 쓴다. 답이 짧고 후보가 여러 개인 프롬프트를 반복해서 던지고, 답이 몇 종류 나오는지 센다.

```
딱 한 글자만 출력해. 설명 금지. 다음 중 하나: 가 나 다 라 마
```

| 관찰 | 판정 |
|---|---|
| 저온에서 전부 같은 답 + 고온에서 두 종류 이상 | 작동 |
| 양쪽 다 골고루 섞임 | 무시됨. 라우트 고정값 |
| 양쪽 다 한 종류 | 온도가 0 근처로 고정 |

판정 기준은 "어떤 답이 나왔나"가 아니라 **"갈렸나"**다. 고온에서 한 글자가 연속으로 나오는 것은 흔하다. 분포가 평평해질 뿐 균등해지지는 않기 때문이다.

> **함정 1**: temperature 0에서도 긴 출력은 매번 다를 수 있다. MoE 배칭 비결정성 때문에 근소하게 붙은 후보가 뒤집히면 그 뒤가 통째로 갈린다. 그래서 짧은 출력으로 잰다.
>
> **함정 2**: 추론 트레이스 길이로 재는 방법은 모델 특성을 심하게 탄다. 어떤 모델은 같은 온도 안에서도 길이가 2.6배까지 벌어져 그룹이 겹친다. 쓰지 않는다.

---

## 3. 파라미터 문법 계열

같은 기능이라도 구현마다 문법이 다르다. 위에서부터 시도한다.

| 우선순위 | 문법 | 계열 |
|---|---|---|
| 1 | `reasoning_effort=max` | OpenAI 호환 최상위 평문 키 |
| 2 | `thinking=json::{"type":"enabled"}` + `reasoning_effort=...` | Z.ai 원형, 켜기와 강도 분리 |
| 3 | `thinking=json::{"type":"enabled","effort":"..."}` | 동거형 |
| 4 | `reasoning=json::{"effort":"..."}` | OpenRouter |
| 5 | `thinking=json::{"budget_tokens":16000}` | 예산 직접 지정 |

RisuAI 추가 파라미터 칸 기준 표기다. 한 줄에 하나씩 넣으며, `json::`은 객체를 넘길 때만 붙인다.

---

## 4. 레이트리밋

답 세기는 호출을 몰아 쏘므로 rpm이 낮은 모델에서 429가 난다. `probe.sh`는 메타데이터에서 rpm을 읽어 `60/rpm × 1.2` 간격을 두고, 429가 나면 `Retry-After`를 따르거나 지수 백오프한다.

rpm 필드명이 다르거나 메타데이터에 없으면 `WS_RPM=n`으로 직접 넘긴다. 실제 필드명을 알아냈으면 `probe.sh` 상단의 후보 목록에 추가해두면 다음부터 자동으로 잡힌다.

---

## 부록 A. 세션 기록

### 2026.08.28 — 초회 조사 (glm-5.2, deepseek-v4-flash-0731)

이 절차는 이날 시행착오에서 나왔다.

**온도가 추론 길이를 늘린다.** 온도를 올리면 추론이 길어진다. 추론 텍스트에는 `Wait`, `Let's try` 같은 백트래킹 토큰이 낮은 확률로 깔려 있는데, 온도가 오르면 한 번 뽑히고 그 뒤로 자기유사적으로 연쇄한다. 선형이 아니라 캐스케이드다.

단, **길어진 것이 깊어진 것은 아니다.** `reasoning_effort`는 학습된 예산 안에서 검증 단계를 늘리지만, 온도로 늘어난 길이는 방황이다. 가지는 많고 가지마다 품질이 낮으며 형식까지 무너진다(temperature 2에서 열지 않은 XML 태그가 출력됐다). 온도로 effort를 대체할 수 없고, 지연시간만 effort처럼 오른다.

**판정 실수 세 건.**

- temperature 2에서 태그가 깨진 것 하나만 보고 온도 작동을 단정했다가, temperature 0에서 출력이 갈리는 것을 보고 뒤집었다. 둘 다 표본 하나짜리 근거였다.
- effort 스윕을 돌렸는데 셸 변수가 비어 있어 측정 전체가 무효였다. 추론 내용이 "사용자가 아직 질문을 안 했다"로 나온 것이 단서였다. 이후 `jq -n --arg`로 교체했다.
- 추론 트레이스 길이로 온도를 재는 방법을 GLM에서 잘 통한다고 일반화했다가 DeepSeek에서 실패했다. 답 세기로 일원화했다.

**엔드포인트마다 기본값이 반대일 수 있다.** GLM-5.2는 `reasoning_effort`를 안 주면 추론이 켜져 있고, DeepSeek V4 Flash는 꺼져 있다. 같은 프록시, 같은 파라미터인데 그렇다.

**2026.07.09 조사 대비 변화.** 당시 구 도메인(`wellspring.encrypt.gay`)의 `glm-5.2-marp` 라우트는 추론이 강제 ON이었고 `thinking` 파라미터를 조용히 무시했다. `deepseek-v3.2-marp`는 반대로 강제 OFF였다. 현재는 값이 라우트까지 도달하며 `none`으로 끌 수 있다.
