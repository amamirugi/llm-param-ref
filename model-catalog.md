# 모델 카탈로그

모델별로 실측 확인된 파라미터 동작. 확인 절차는 `endpoint-probing.md`, 자동화는 `probe.sh`.

기재는 실측된 것만 한다. 미확인 항목은 비워두고 추측으로 채우지 않는다.

---

## api.wellspring.encrypt.gay

### glm-5.2

- 확인일: 2026.08.28
- 온도: **무시됨**
- 추론 문법: `reasoning_effort` (최상위 평문 키)
- 허용값: `none` `low` `high` `max`
- 추론 끄기: 가능 (`none`). 미지정 시 기본은 ON
- 단계 조절: **구분되지 않음**
- 파라미터 필터링: 없음 (값 검증 후 에러 반환)
- rpm: 5
- 툴콜: **미지원** (`supports_tools: false`)
- 토큰 상한: 출력 32768 / 입력 49152

추론 조절 (RisuAI 추가 파라미터 칸. 값만 갈아끼우면 됨):

```
reasoning_effort=none
```

**조절할 수 있는 것이 사실상 이것뿐이다.** 온도도 단계도 반응하지 않으므로 켜기/끄기만 선택한다.

측정치 — 온도 (답 세기, `reasoning_effort=none`, "아무 한글 명사 하나만"):

| temperature | 결과 |
|---|---|
| 0 | 사과 ×5 (1회 호출 실패) |
| 2 | 사과 ×6 |

어휘 전체가 후보인 개방형 프롬프트에서 유효 표본 11개가 전부 동일하다. 추론을 켠 상태(`low`)에서도, `가 나 다 라 마` 선택형 프롬프트에서도 같은 결과였다. temperature 2까지 올려도 갈리지 않는다.

측정치 — effort (temperature 0.7, max_tokens 16000~30000, 5회씩):

| effort | 측정치 | 범위 | 평균 |
|---|---|---|---|
| none | 0, 0, 0, 0, 0 | — | 0 |
| low | 2944, 1260, 754, 2110, 4802 | 754–4802 | 2374 |
| high | 1416, 5297, 1785, 286, 269 | 269–5297 | 1811 |
| max | 2382, 2123, 3083, 6575, 2305 | 2123–6575 | 3294 |

**세 구간이 거의 완전히 겹치고 평균 순서도 뒤집혀 있다**(high가 최저). 출력 상한 32768에 못 미치므로 절단 때문도 아니다. 단계 간 차이가 있다 해도 반복 편차에 완전히 묻힌다.

특이사항:
- `xhigh` 없음. OpenRouter 문서의 `high`/`xhigh` 체계와 다르다.
- `reasoning=json::{...}` 형식 거부.
- 온도와 단계가 모두 반응하지 않는 반면 `none` 스위치는 5/5 재현된다. **이 라우트는 on/off만 통과시키고 정도를 조절하는 값은 고정하는 것으로 보인다** (추정).
- 툴콜을 못 쓰므로 에이전트 자리에 넣을 수 없다. 구조화 출력은 별개로 가능.

> **철회된 판정 (2026.08.28 오전)**: 추론 트레이스 길이가 temp 0에서 짧고 1.2에서 길다는 관찰로 "온도 작동"이라 결론냈으나, 이후 같은 온도에서도 트레이스 길이가 8배 흔들리는 것이 확인되어 근거가 무효화되었다. temperature 2에서 XML 태그가 깨진 관찰도 표본 하나였다.

### deepseek-v4-flash-0731

- 확인일: 2026.08.28
- 온도: **작동** (추론 OFF 기준)
- 추론 문법: `reasoning_effort` (최상위 평문 키)
- 허용값: `none` `low` `high` `max`
- 추론 끄기: 가능 (`none`). **미지정 시에도 OFF가 기본**
- 단계 조절: **작동** (low와 max가 10배 차이)
- 파라미터 필터링: 없음 (값 검증 후 에러 반환)
- rpm: 10

추론 조절 (RisuAI 추가 파라미터 칸. 값만 갈아끼우면 됨):

```
reasoning_effort=none
```

측정치 — effort (temperature 0.7, max_tokens 16000, 3회씩):

| effort | 1회 | 2회 | 3회 | 범위 |
|---|---|---|---|---|
| none | 0 | 0 | 0 | — |
| low | 2966 | 4779 | 2306 | 2.3k–4.8k |
| high | 60050 | 72487 | 2673 | 불안정 |
| max | 65464 | 48935 | 86633 | 49k–87k |

**low와 max는 구간이 전혀 겹치지 않는다.** low 최댓값 4779, max 최솟값 48935로 약 10배 차이다.

측정치 — 온도 (답 세기, 2회 반복):

| effort | temp | 결과 |
|---|---|---|
| none | 0 | 구름 구름 구름 구름 / 구름 구름 바람 구름 |
| none | 1.5 | 연필 구름 구름 바람 / 안녕하세요.모래시계 구름 구름 |
| low | 0 | 사랑 바람 사랑 사과 / 사과 바람 사과 사랑 |
| low | 1.5 | 별 사과 지우개 바람 / 구름 나무 바다 바람 |

`none`에서 temp 0은 8개 중 7개가 동일하고 1.5에서는 흩어진다. 온도 작동이다. 1.5에서 `안녕하세요.모래시계`처럼 **지시를 어기는 출력이 나온다** — 형식 제약이 있는 작업에서 1.5는 위험하다.

특이사항:
- **`max`는 지연시간 폭탄이다.** 추론만 5만–8만 자.
- **`high`는 이봉으로 보인다.** 3회 중 1회가 2673, 나머지가 60k대로 중간값이 없다. 과추론 루프에 걸리면 폭주하고 아니면 짧게 끝난다. 켜서 쓸 단계가 못 된다.
- 결론: **`none` 또는 `low`만 쓴다.** high 이상은 제외.
- GLM과 반대로 기본값이 추론 OFF다.
- **추론 트레이스 길이로는 온도 판정이 안 된다.** 같은 온도 안에서도 길이가 2.6배까지 벌어져(temperature 0에서 2597 vs 6679) 그룹이 겹친다.
- 답 세기를 몰아 쏘면 429가 난다. 7초 간격 필요.

> **철회된 측정 (2026.08.28, max_tokens 4000)**: none 0 / low 2424 / high 15579 / max 17008으로 측정되었으나, `finish=length`로 추론이 잘린 값이다. 상한에 눌려 high와 max가 실제보다 훨씬 작게 나왔다. **effort 측정 시 max_tokens를 충분히 크게 잡아야 한다.**

> **미확인**: `low`에서 온도가 먹는지는 판정하지 못했다. 추론이 켜지면 temp 0에서도 출력이 갈리는데, 이는 추론 사슬 자체가 비결정적이라 기준선이 흔들리기 때문이다. **"온도가 무시된다"는 뜻이 아니라 이 방법으로는 못 잰다는 뜻이다.**

---

## 둘을 비교하면

| | glm-5.2 | deepseek-v4-flash-0731 |
|---|---|---|
| 온도 | 무시됨 | 작동 (추론 OFF 기준) |
| 추론 기본값 | ON | OFF |
| 추론 켜고 끄기 | 가능 | 가능 |
| 단계 조절 | 구분되지 않음 | 작동 (10배 차) |
| 추론량 (low) | ~0.8–4.8k | ~2.3–4.8k |
| 추론량 (max) | ~2.1–6.6k | ~49–87k |
| 툴콜 | 미지원 | 미확인 |
| rpm | 5 | 10 |
| 조절 가능한 것 | 추론 on/off | 추론 on/off, 단계, 온도 |

같은 프록시, 같은 파라미터 문법인데도 온도도 기본값도 단계 반응도 정반대다. 모델을 바꿀 때마다 재측정해야 하는 이유다.

---

## 신규 항목 템플릿

```markdown
### <모델 id>

- 확인일: YYYY.MM.DD
- 온도: 작동 / 무시됨 / 미확인
- 추론 문법: <키 이름과 계열>
- 허용값: <에러 메시지에서 캔 목록>
- 추론 끄기: 가능 / 불가. 미지정 시 기본값은?
- 단계 조절: 작동 / 구분되지 않음
- 파라미터 필터링: 없음 / 있음
- rpm: <메타데이터에서 확인한 값>
- 툴콜: 지원 / 미지원

추론 조절 (RisuAI 추가 파라미터 칸. 값만 갈아끼우면 됨):

<코드블럭 하나>

측정치 — effort (temperature ? 고정, max_tokens 충분히 크게, 3회 이상):

| effort | 1회 | 2회 | 3회 |
|---|---|---|---|

측정치 — 온도 (답 세기, 추론 OFF, 개방형 프롬프트):

| temperature | 결과 |
|---|---|

특이사항:
```
