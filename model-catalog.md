# 모델 카탈로그

모델별로 실측 확인된 파라미터 동작. 확인 절차는 `endpoint-probing.md`, 자동화는 `probe.sh`.

기재는 실측된 것만 한다. 미확인 항목은 비워두고 추측으로 채우지 않는다.

---

## api.wellspring.encrypt.gay

### glm-5.2

- 확인일: 2026.08.28
- 온도: **무시됨** (라우트 고정값으로 보임)
- 추론 문법: `reasoning_effort` (최상위 평문 키)
- 허용값: `none` `low` `high` `max`
- 추론 끄기: 가능 (`none`). 미지정 시 기본은 ON
- 단계 조절: **불가** (아래 참조)
- 파라미터 필터링: 없음 (값 검증 후 에러 반환)
- rpm: 5
- 툴콜: **미지원** (`supports_tools: false`)
- 토큰 상한: 출력 32768 / 입력 49152

추론 조절 (RisuAI 추가 파라미터 칸. 값만 갈아끼우면 됨):

```
reasoning_effort=none
```

측정치 (답 세기, temperature 0 vs 2):

| 프롬프트 | temp 0 | temp 2 |
|---|---|---|
| `가 나 다 라 마` 중 한 글자 | 가 가 가 가 | 가 가 가 가 |
| 아무 한글 명사 (추론 OFF) | 사과 사과 사과 | 사과 사과 사과 |
| 아무 한글 명사 (추론 low) | 사과 사과 | 사과 사과 |

어휘 전체가 후보인 개방형 프롬프트에서 temp 2가 갈리지 않았다. 추론 ON/OFF 양쪽에서 동일하다.

측정치 (effort 스윕, temperature 0.7, 2회씩):

| effort | rlen 1회 | rlen 2회 |
|---|---|---|
| none | 0 | 0 |
| low | 2944 | 1260 |
| high | 1416 | 5297 |
| max | 2382 | 2123 |

특이사항:
- `xhigh` 없음. OpenRouter 문서의 `high`/`xhigh` 체계와 다르다.
- `reasoning=json::{...}` 형식 거부.
- **low/high/max 간 서열은 판정 불가.** 같은 단계 반복 측정에서 rlen이 최대 8배(high 1416↔5297) 흔들려, 단계 간 차이가 반복 편차에 묻힌다. 켜고 끄는 것만 유효하다.
- 툴콜을 못 쓰므로 에이전트 자리에 넣을 수 없다. 구조화 출력은 별개로 가능.

> **철회된 판정 (2026.08.28 오전)**: 추론 트레이스 길이가 temp 0에서 짧고 1.2에서 길다는 관찰로 "온도 작동"이라 결론냈으나, 이후 같은 온도에서도 트레이스 길이가 8배 흔들리는 것이 확인되어 근거가 무효화되었다. temperature 2에서 XML 태그가 깨진 관찰도 표본 하나였다.

### deepseek-v4-flash-0731

- 확인일: 2026.08.28
- 온도: **작동** (답 세기 방식)
- 추론 문법: `reasoning_effort` (최상위 평문 키)
- 허용값: `none` `low` `high` `max`
- 추론 끄기: 가능 (`none`). **미지정 시에도 OFF가 기본**
- 단계 조절: 불확실 (편차 큼)
- 파라미터 필터링: 없음 (값 검증 후 에러 반환)
- rpm: 10

추론 조절 (RisuAI 추가 파라미터 칸. 값만 갈아끼우면 됨):

```
reasoning_effort=low
```

측정치 (effort 스윕, temperature 0.7, max_tokens 4000):

| effort | completion_tokens | reasoning 길이 |
|---|---|---|
| none | 919 | 0 |
| low | 3527 | 2424 |
| high | 6829 | 15579 |
| max | 8000 | 17008 |

측정치 (답 세기, `가 나 다 라 마` 중 한 글자):

| temperature | 결과 |
|---|---|
| 0 | 가 가 가 가 가 가 |
| 1.5 | 마 마 마 가 / 가 마 가 가 |

특이사항:
- **추론량이 극단적.** low→high에서 6.4배로 폭발한다. high 이상은 지연시간 부담이 크므로 파이프라인 투입 시 `low`까지만 쓴다. 단, 1회 측정이므로 단계 간 서열은 확정이 아니다.
- max의 completion_tokens가 정확히 8000. 요청한 max_tokens(4000)를 넘겼으므로 추론 토큰이 상한에 안 잡히거나 라우트 상한 8000이 걸린 것으로 보인다. **max의 rlen 17008은 하한값**이며 실제로는 더 클 수 있다.
- GLM과 반대로 기본값이 추론 OFF다.
- **추론 트레이스 길이로는 온도 판정이 안 된다.** 같은 온도 안에서도 길이가 2.6배까지 벌어져(temperature 0에서 2597 vs 6679) 그룹이 겹친다.
- 답 세기를 몰아 쏘면 429가 난다. 7초 간격 필요.

---

## 둘을 비교하면

| | glm-5.2 | deepseek-v4-flash-0731 |
|---|---|---|
| 온도 | 무시됨 | 작동 |
| 추론 기본값 | ON | OFF |
| 추론량 | 중간, 편차 큼 | 극단적 |
| 툴콜 | 미지원 | 미확인 |
| rpm | 5 | 10 |

같은 프록시, 같은 파라미터 문법인데도 온도와 기본값이 정반대다. 모델을 바꿀 때마다 재측정해야 하는 이유다.

---

## 신규 항목 템플릿

```markdown
### <모델 id>

- 확인일: YYYY.MM.DD
- 온도: 작동 / 무시됨 / 미확인
- 추론 문법: <키 이름과 계열>
- 허용값: <에러 메시지에서 캔 목록>
- 추론 끄기: 가능 / 불가. 미지정 시 기본값은?
- 단계 조절: 가능 / 불가 / 불확실
- 파라미터 필터링: 없음 / 있음
- rpm: <메타데이터에서 확인한 값>
- 툴콜: 지원 / 미지원

추론 조절 (RisuAI 추가 파라미터 칸. 값만 갈아끼우면 됨):

<코드블럭 하나>

측정치 (temperature ? 고정):

| effort | completion_tokens | reasoning 길이 |
|---|---|---|

특이사항:
```
